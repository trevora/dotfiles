[core]
	editor = /usr/bin/vim

[push]
	default = simple

[alias]
    conflicted = !vim +Conflicted

[init]
    defaultBranch = main

# Allow local customizations in the .gitconfig_local file
[include]
    path = ~/.gitconfig_local
